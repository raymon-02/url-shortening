url:
  generate:
    preset: 500000

hazelcast:
  members:
    - "localhost:5701"
    - "localhost:5702"
    - "localhost:5703"
  redoOperation: true
  connectionTimeout: 0
  connectionAttempts: 5

---

spring.profiles: docker

hazelcast:
  member: "hazelcast-service:5701"
